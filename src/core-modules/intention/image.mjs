import CoreModule from '../../core-module.mjs';

export default class ImageIntentionCoreModule extends CoreModule {
  /**
   *
   * @param {Core} core
   */
  constructor(core) {
    super(core);
  }
}
