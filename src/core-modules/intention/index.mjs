import CoreModule from '../../core-module.mjs';

export default class IntentionCoreModule extends CoreModule {
  /**
   *
   * @param {Core} core
   */
  constructor(core) {
    super(core);
    
  }
}
